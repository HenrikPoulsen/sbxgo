# Docker image IDs written by sbxgo setup, as local build artifacts not for version control.
.image-id
.image-id-new

# Hash of create-time config recorded by sbxgo to detect drift on resume.
.create-state

# Temporary tar archives produced when loading Docker templates into sbx.
*.tar
